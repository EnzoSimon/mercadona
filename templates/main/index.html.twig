{% extends 'base.html.twig' %}

{% block body %}
    <header>
        <img src="assets/uploads/5fc93aedbe500e8f2cbd84f5f0243f74.webp" alt="header" class="img-header img-responsive">
    <header>
    <main class="container">
        <section class="row">
            <div class="col-12 align-center">
                <h1>Mercadona</h1>
                <p>Votre destination shopping incontournable.</p>            
            </div>
            {% for category in categories %}
                {% if category.parent == null %}
                    <div class="col-12">
                        <h2>{{ category.name }}</h2>
                    </div>
                {% else %}
                    <div class="col-4">
                        <div class="card h-100">
                            <div class="card-body">
                                <h3 class="card-title">{{ category.name }}</h3>
                                <a href="{{ path('categories_list', {slug: category.slug}) }}" class="btn btn-primary">Voir les produits</a>
                            </div>
                        </div>
                    </div>    
                {% endif %}
            {% endfor %}
        </section>
    </main>
{% endblock %}
